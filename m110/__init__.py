"""M110 — engine package (headless).

The PySide6 UI in `m110.ui` imports this package in-process. Engine
modules are ported incrementally from the Astronomy `scripts/` workflow.
"""
__version__ = "0.2.0b8"
